

1) Proper camera controls.

2) Write a general instanced renderer that takes all the objects it needs to draw at each frame and draw them. Now for each "type" of object
I have an instancedSet object that draws them.

